# Spacing and Wrapping
softWrap = false
softTabs = true
tabSize = 2

# Fontsize
fontSize = 11
scmStatus = false

includeFiles = "{*,.tm_properties,.ember-cli,.yarnrc,.npmignore,.eslintrc,.htaccess,.gitignore,.gitkeep,.rspec,.travis.yml,.ruby-version,.rbenv-version,.rubocop.yml,.env,.env.development,.env.test,.env.local,.env.development.local,.env.production,.env.test.local,.watchmanconfig,.jshintrc,.keep,.env.demo,.env.demo.local,.babelrc,.postcssrc.yml}"

excludeFiles = "{tags,log/development.log,log/staging.log,log/production.log,coverage/*}"
excludeDirectories = "{$excludeDirectories,tmp}"

theme = "82116FDC-DA11-4407-9A87-7A2E677C3686"

[ *.md ]
spellChecking = true
spellingLanguage = "en"

[ .git/COMMIT_EDITMSG ]
softWrap         = true
spellChecking    = true
spellingLanguage = "en"
