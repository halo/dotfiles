softWrap = false
softTabs = true
tabSize = 2

fontSize = 11
scmStatus = false

includeFiles = "{*,.tm_properties,.htaccess,.gitignore,.gitkeep,.rspec,.travis.yml,.ruby-version,.rbenv-version,.htaccess}"

excludeFiles = "{tags,log/development.log,log/staging.log,log/production.log,coverage/*}"
excludeDirectories = "{$excludeDirectories,tmp}"

[ .git/COMMIT_EDITMSG ]
softWrap         = true
spellChecking    = true
spellingLanguage = 'en'
