[user]
  # Keep it local to each repo to avoid mistakes.

[core]
  # May be overriden by $GIT_EDITOR
  editor = mate -w

[color]
  status = auto
  branch = auto
  diff = auto

[alias]
  uncommit = reset --soft HEAD^
  unstage = reset HEAD .
